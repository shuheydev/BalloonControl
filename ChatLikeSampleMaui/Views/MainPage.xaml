<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ChatLikeSampleMaui.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ChatLikeSampleMaui.Controls">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            VerticalOptions="Center">
            <Button Command="{Binding ChangeMessageCommand}" Text="Change Message" />
            <!--  ボタンクリックでテキストが変わる  -->
            <controls:Balloon BalloonColor="LightBlue" Text="{Binding Message}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
